const props = {
    openRolesData: ["frontend engineer", "backend engineer", "ux/ui designer", "product manager", "cto", "ceo", "coo"]
}

console.log(props.openRolesData);

let arr = [1, 2, 3]
arr.map((element)=>(console.log("Hello Map: ", element)))